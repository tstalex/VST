<% content_for :head do %>
  <%= javascript_include_tag 'ext_scaffold/tarif_calculation.js' %>
  <% javascript_tag do %>
    Ext.Ajax.defaultHeaders = { 'Content-Type': 'application/json' };
    Ext.onReady(function() {

      Ext.state.Manager.setProvider(new Ext.state.CookieProvider());
      Ext.QuickTips.init();

      var vp = new Ext.Viewport({
        layout: 'fit',
        renderTo:"main",
        items:[TarifCalculation.tarif_calculationPanel()]
      });
    });
  <% end %>
<% end %>
<div id="main"></div>